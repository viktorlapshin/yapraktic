import { Matcher } from "anymatch";
export declare type CamelCaseOptions = boolean | "asIs" | "camelCase" | "camelCaseOnly" | "dashes" | "dashesOnly" | "only";
export declare const LocalScope = "local";
export declare type Scope = typeof LocalScope | "global" | "pure";
export interface SettingsObject {
    postcssConfigDir: string;
    baseDir: string;
    camelCase: CamelCaseOptions;
    defaultScope: Scope;
    include: Matcher;
    exclude: Matcher;
}
export interface ParseResult {
    classes: ReadonlyMap<string, string[]>;
    usedClasses: ReadonlySet<string>;
}
